# Build output, vendored trees and the separate Docusaurus site.
js/
dist/
build/
node_modules/
vendor/
coverage/
coverage-frontend/
coverage-vitest/
docs/
*.min.*

# Generated by the translation workflow — `l10n/*.js` is emitted by the
# Nextcloud l10n tooling (`OC.L10N.register(...)`, four-space indent). Letting
# prettier own it would make the formatter and the translation sync rewrite the
# same 37 files in opposite directions on every run.
l10n/

# Build-time tooling, not app code. Vendored from openregister and kept in sync
# by hand, so reformatting diverges the copies line for line. eslint skips this
# tree too — see the matching `ignores` entry in eslint.config.js.
scripts/
