# Tasks: hitl-approval-rule-action (superseded)

The original 16-task / 42-checkbox list is removed with the 2026-09-02
re-scope (see proposal.md for the full disposition; the list survives in
`archive/2026-07-15-hitl-approval-rule-action/tasks.md`, where 31/42 boxes are
checked with per-task evidence, and in git history). Work items live in the
successor changes:

- `approvals-verification-pack` (ready to start)
- `hitl-on-shared-tasks` (in flight, first PR #1812 merged)

`design.md` and `specs/approval-workflow/spec.md` stay in this directory as
`@spec` anchor targets. There is nothing to implement from this change
directly.
