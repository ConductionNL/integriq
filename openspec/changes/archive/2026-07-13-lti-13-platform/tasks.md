# Tasks — LTI 1.3 / LTI Advantage adapter (Platform + Tool roles)

> Implementation order follows the data model outward: register schema first
> (nothing else compiles/tests without it), then the service layer
> (`lib/Service/Lti/*`, ADR-008 Controller → Service → Mapper), then the
> public HTTP surface, then the tenant-wide key-management UI, then tests,
> then docs. Key generation/rotation (Phase 4) depends on `LtiKeyService`
> from Phase 2, so it is sequenced after the service layer even though the
> user-facing "key mgmt" grouping is called out separately per the brief.

## 1. Register schema (openconnector-register-schema REQ-A-001/002, lti-platform REQ-LTI-001)

- [x] 1.1 Add `lti_platform` schema to `lib/Settings/openconnector_register.json`: `issuer`, `clientId` (our assigned id), `authLoginUrl`, `authTokenUrl`, `jwksUri`, `signingKeys[]` (`kid`, `algorithm`, `publicJwk`, `privateKeySecret`, `status`, `rotatedAt` — REQ-LTI-002 shape), timestamps
- [x] 1.2 Add `lti_tool` schema: `clientId` (assigned to the tool), `oidcLoginUrl`, `launchUrl`, `jwksUri`, `signingKeys[]` (same shape as 1.1), timestamps
- [x] 1.3 Add `lti_deployment` schema: `deploymentId` (LTI claim value), `ltiPlatformId`/`ltiToolId` (exactly-one relation, both nullable, validated per REQ-LTI-001 scenario 2), `launchTargetUrl`, `gradeSink` (`{targetType, targetId}`), `rosterSource` (`{targetType, targetId}`), `mappingId`
- [x] 1.4 Register all three under `components.registers[openconnector].schemas`. HEAD reality check: at the time this change was implemented, `components.schemas` already had 17 entries (`peppol_transmission` had been added since the proposal was drafted) but the `registers[openconnector].schemas` LIST still only had 16 — `peppol_transmission` was defined but never registered, a pre-existing drift bug. Fixed alongside this change (added `peppol_transmission` to the list) + added the 3 LTI schemas, so both `components.schemas` and the registers list now agree at **20** (not the proposal's stated 16→19 — flagged as a HEAD-drift conflict in the apply report, not silently reinterpreted).
- [x] 1.5 OR schema-level `oneOf` (two branches, each `required` + `not.required` the other) enforcing `lti_deployment` references exactly one of `ltiPlatformId`/`ltiToolId` at save time — verified `oneOf` is a real, enforced keyword in `openregister-dev/lib/Service/Object/ValidateObject.php` (sibling checkout), not a speculative keyword. `LtiDeploymentResolverService` (used by launch/AGS/NRPS dispatch) additionally guards defensively against a malformed row that predates the schema constraint.
- [x] 1.6 No seed rows added for `lti_platform`/`lti_tool`/`lti_deployment` — empty-by-default, matching every other catalogue-style mutable schema; `x-openregister-seed: []` on all three.
- [ ] 1.7 `openspec sync` of the `openconnector-register-schema` delta into `openspec/specs/openconnector-register-schema/spec.md` NOT done — left for the archive/sync step per this repo's OpenSpec workflow (apply agents implement code, `/opsx-sync` merges spec deltas); the actual count implemented is 17→20 (see 1.4 note), which the synced spec must reflect, not the proposal's 16→19.

## 2. Backend services (`lib/Service/Lti/*`, ADR-008 Controller → Service → Mapper)

- [x] 2.1 `LtiKeyService`: generate a fresh RS256/PS256 keypair and set `active` (REQ-LTI-002); rotate (`active`→`previous`, new `active`, 7-day grace window); a scheduled sweep (`LtiKeyRetirementJob`, mirroring `EventRetryJob`'s cron-registration pattern, hourly) that flips `previous`→`retired` past the grace window and drops `retired` keys from the published set. Key material is generated via `openssl_pkey_new`/`openssl_pkey_export` and stored as base64(PEM) — this exact shape is directly consumable by `AuthenticationService::fetchJWTToken()`'s existing `getRSJWK()` path (needed unmodified for 2.9/2.11's outbound reuse), and the public JWK is derived from it via the same secure temp-file (`tempnam`+`chmod 0600`+`try/finally`) pattern `AuthenticationService::getRSJWK()`/`AuthorizationService::getJWK()` already use (#1012 fix).
- [x] 2.2 `LtiJwksResolverService` (REQ-LTI-003): resolve `jwks_uri`→`JWKSet` via the existing `CallService`/`http-call-engine` machinery (ephemeral, non-persisted `ObjectEntity` Source — no `Source` row created); cache under `ICacheFactory::createDistributed('openconnector.lti.jwks')` keyed `jwks:<registrationType>:<registrationUuid>`, 1h default TTL; `kid` lookup; on-miss refetch guarded to at most once per 60s per registration via a `jwks:refetch:<registrationUuid>` cache key; fail closed (reject) while the guard is active
- [x] 2.3 `LtiLaunchService::validateLaunch()` (REQ-LTI-005): JWS signature verification against a REQ-LTI-003-resolved JWK; delegate `iat`/`exp`/`nbf` to the existing `AuthorizationService::validatePayload()` (no reimplementation); `aud`/`azp` match; `nonce` presence + atomic get-then-delete consume from `openconnector.lti.nonce` (new namespace, distinct from `openconnector.jti`); `deployment_id` claim resolves to a registered `lti_deployment` under the token's `lti_platform`; `message_type`/`version` (`"1.3.0"`) present and recognised; any failure is a hard 401/400 reject, no partial-trust path. Algorithm-confusion guard mirrors `AuthorizationService::authorizeJwt()`'s pinned-`alg` check.
- [x] 2.4 `LtiLaunchService`: OIDC login-initiation logic (REQ-LTI-004) — validate `iss`/`client_id` against a registered `lti_platform`, generate `state`+`nonce`, persist `nonce` under `openconnector.lti.nonce` (`nonce:<registrationUuid>:<nonce>`, 10-min TTL), `state` round-tripped via `SameSite=None; Secure` cookie (set in `LtiController::login()`)
- [x] 2.5 `LtiLaunchService`: Platform-role launch initiation (REQ-LTI-006) — third-party-initiated login redirect to the `lti_tool`'s `oidc_login_url`, then an auto-submitting form POSTing an `id_token` signed with that registration's `active` key (via `LtiKeyService`) to `launch_url`; `message_type` = `LtiResourceLinkRequest` or `LtiDeepLinkingRequest`. NOTE: the "third-party-initiated login redirect" half is the browser-redirect chain a consuming app performs using the returned `formActionUrl`/`idToken` — this service builds and signs the artifact; it does not itself drive a browser.
- [x] 2.6 `LtiLaunchService`: Deep Linking 2.0 both directions (REQ-LTI-006) — construct+sign `LtiDeepLinkingResponse` (Tool role) POSTed to `deep_link_return_url`; verify an inbound `LtiDeepLinkingResponse` identically to 2.3 (Platform role) and hand parsed content items back to the initiating consuming app
- [x] 2.7 `LtiAgsService`: `POST /api/lti/token` RFC 7523 JWT-bearer client-credentials grant (REQ-LTI-007) — resolve issuing `lti_tool` by `iss`/`sub`, verify assertion signature via 2.2 (through `LtiLaunchService::verifyIdTokenSignature()`, made public + reused rather than duplicated), issue a short-lived access token scoped to one `lti_deployment` + requested AGS/NRPS scope(s) only (never cross-deployment). PROTOCOL EXTENSION FLAGGED: RFC 7523 has no deployment-selection mechanism, so the token request additionally requires a `deployment_id` form parameter (this instance's `lti_deployment` UUID) — needed to satisfy design.md D8's per-deployment token scoping, which goes beyond the base RFC.
- [x] 2.8 `LtiAgsService`: inbound line-item read / score-POST enforcement of the `.../lineitem` / `.../score` scopes from 2.7's tokens; translate a received score into a `nl.conduction.lti.ags.score.received` CloudEvent published via the existing `EventService::emitCloudEvent()` → `processEvent()` (unmodified) — no direct write to `lti_deployment.gradeSink`. Line-item read is a scope+deployment check only (no line-item CRUD is modelled — line items are consuming-app-owned data per the adapter's "emit, don't own" posture, design.md D7).
- [x] 2.9 `LtiAgsService`: Tool-role outbound score publish / result read (REQ-LTI-008) reusing `AuthenticationService::fetchOAuthTokens()` with `client_assertion_type=urn:ietf:params:oauth:client-assertion-type:jwt-bearer` against the `lti_platform`'s `auth_token_url`, dispatched through the existing outbound HTTP call machinery (CallLog observability, no silent-drop on failure). NOTE: `fetchOAuthTokens()`'s token-endpoint request itself uses a raw Guzzle client (pre-existing, unmodified) with no CallLog; a token-endpoint failure is never silently dropped (logged + rethrown as a 502), while the subsequent AGS REST call (score POST / result GET) IS dispatched via `CallService` and IS CallLog-observable — see LtiAgsServiceTest for the exact assertion.
- [x] 2.10 `LtiNrpsService`: inbound roster read (REQ-LTI-009, Platform role) — dispatch `lti_deployment.rosterSource` as an ADR-008 `targetType: 'register/schema'` read through the same mapper path `EndpointService::handleSchemaRequest()` uses (`$this->objectService->getMapper(...)` + `findAllPaginated()`, `lib/Service/EndpointService.php:1063/987`), transform via the deployment's mapping into the IMS Names/Roles JSON shape (falls back to a best-effort default shape when no mapping is configured), return synchronously (no CloudEvent)
- [x] 2.11 `LtiNrpsService`: Tool-role outbound roster pull reusing the same JWT-bearer client-credentials mechanism as 2.9 (via `LtiAgsService::pullResourceForDeployment()`, extracted to avoid duplicating the token-exchange+dispatch shape), scoped to `.../contextmembership.readonly`
- [x] 2.12 `nl.conduction.lti.ags.score.received` CloudEvent type constant added as `LtiAgsService::AGS_SCORE_RECEIVED_EVENT_TYPE`, matching the existing convention (inline string constants on the owning service, e.g. `PeppolTransmissionService`) rather than a shared registry file (none exists); confirmed `events-cloudevents` REQ-001's existing type-matching fan-out picks it up with zero changes to `EventService` (verified: `EventService::emitCloudEvent()`/`processEvent()` are untouched by this change).

## 3. Endpoints (`lib/Controller/LtiController.php`, `appinfo/routes.php`)

- [x] 3.1 `LtiController` mirroring `DSOController`'s dedicated-controller shape (not the generic `Endpoint` pipeline): `login()`, `launch()`, `token()`, AGS line-item/score actions, NRPS membership action, JWKS-publish action — thin controllers delegating to the Phase 2 services
- [x] 3.2 `appinfo/routes.php`: `GET/POST /api/lti/{deployment}/login`, `POST /api/lti/{deployment}/launch`, `POST /api/lti/token`, AGS line-item/score routes, NRPS membership route, `GET /.well-known/lti/{registrationType}/{registrationUuid}/jwks.json` — every route carries an explicit `#[PublicPage]`/`#[NoCSRFRequired]` posture on the protocol endpoints and `#[AuthorizedAdminSetting]` on the key-management endpoints (hydra-gate-route-auth)
- [x] 3.3 JWKS-publish endpoint (REQ-LTI-002): returns `active` + any `previous` (grace-window) public keys as a JWKS document per registration; `retired` keys never appear
- [x] 3.4 Reject-before-redirect behaviour on `login()` (REQ-LTI-004 scenario: unregistered `iss`/`client_id` → HTTP 400, no redirect, no nonce persisted) and hard-reject behaviour on `launch()`/`token()`/AGS/NRPS (401/400/403 per the specific failure, no partial-trust fallback anywhere in this controller)
- [x] 3.5 Per-deployment access-token scope enforcement at the AGS/NRPS route layer (REQ-LTI-007 scenario: a token scoped to deployment A rejected with 403 against deployment B's endpoints) — `LtiAgsService::assertScopedToDeployment()`, called from every AGS/NRPS controller action

## 4. Key management (tenant-wide, Beheer > Authenticatie + Adapters catalogue)

- [x] 4.1 Admin-gated, CSRF-protected endpoints to generate/rotate a registration's signing keys (mirrors `webhook-signing` REQ-WHS-002's generate/rotate shape, `EventsController::generateSigningSecret()`/`rotateSigningSecret()`) — `LtiController::generateKey()`/`rotateKey()` wired to `LtiKeyService` (2.1)
- [ ] 4.2 *Beheer > Authenticatie* UI: registration list (both `lti_platform` and `lti_tool`) + per-registration key lifecycle view (active `kid`, previous `kid` + grace-window countdown, rotate action) per ADR-017 Rule 3/7 — NOT built. This is a Vue/manifest frontend surface; out of capacity for this apply pass (backend contract it would call — `generateKey`/`rotateKey`/`jwks` — is complete and independently testable).
- [ ] 4.3 *Adapters* catalogue card: "LTI 1.3 / LTI Advantage" entry (discovery, capability description) — NOT built (frontend, same capacity note as 4.2).
- [ ] 4.4 *Verbindingen* new-connection wizard step for registering an `lti_platform`/`lti_tool`/`lti_deployment` — NOT built (frontend, same capacity note as 4.2). Registrations are fully creatable today via OR's generic `/api/objects/openconnector/{lti_platform,lti_tool,lti_deployment}` API (chain-C architecture), just without a dedicated wizard step.
- [x] 4.5 Own-key custody note surfaced in code/docblocks everywhere the key material lives (plaintext-pending-encryption per ADR-007, same as every other `Source` secret) — see `LtiKeyService` class docblock + `signingKeys.privateKeySecret` schema description; NOT surfaced in end-user UI copy since no UI was built (4.2/4.3/4.4 deferred).

## 5. Tests

- [x] 5.1 PHPUnit: `LtiKeyServiceTest` (6 tests) — generation, rotation (active→previous→retired timing), grace-window JWKS content, published-set never contains `retired` keys
- [x] 5.2 PHPUnit: `LtiJwksResolverServiceTest` (5 tests) — cache hit, cache miss + refetch, 60s refetch-guard (exactly one outbound fetch under repeated unknown-`kid` requests), per-registration cache namespacing (two registrations sharing a `jwks_uri` do not cross-poison), invalid-URI fails closed without reaching the call machinery
- [x] 5.3 PHPUnit: `LtiLaunchServiceTest` (17 tests) — valid login-initiation redirect (state+nonce present, 400 on unregistered issuer with no redirect/no persisted nonce), valid launch redirect, nonce-replay rejection (401), unregistered `deployment_id` rejection (400), wrong-audience rejection (401), wrong-issuer rejection (400), expired-token rejection (401, via the reused `AuthorizationService::validatePayload()` path), unknown-kid rejection (401), forged-signature rejection (401), algorithm-confusion rejection (401), missing-message_type rejection (400)
- [x] 5.4 PHPUnit: Deep Linking (in `LtiLaunchServiceTest`) — Platform-role launch signed with the correct `lti_tool` active key, `buildDeepLinkingResponse()` signed with the platform's active key and carries content items, inbound `LtiDeepLinkingResponse` verified before content items are returned, rejected on wrong message_type
- [x] 5.5 PHPUnit: `LtiAgsServiceTest` (11 tests) — token-endpoint client-assertion exchange (valid + mismatched iss/sub + disallowed scope + deployment-not-owned-by-tool), deployment-scoped token issuance, cross-deployment 403, score-received CloudEvent published with correct type/payload and NOT written to `gradeSink` directly (+ zero-subscriber success case, REQ-LTI-010), Tool-role outbound score publish reusing `fetchOAuthTokens()`, token-endpoint failure surfaced as a propagated exception (never silently dropped — see 2.9 note on CallLog scope)
- [x] 5.6 PHPUnit: `LtiNrpsServiceTest` (3 tests) — roster request served via the ADR-008 `register/schema` read path (asserts `getMapper()` called with the parsed register/schema ids and zero `emitCloudEvent` calls — synchronous, no CloudEvent), missing-scope request rejected 403, unconfigured `rosterSource` rejected 400
- [x] 5.7 PHPUnit: consuming-app placement contract (REQ-LTI-010) — `LtiAgsServiceTest::testScoreReceivedSucceedsWithZeroSubscribers` asserts an `lti_deployment` with zero matching subscriptions still succeeds (CloudEvent created, `messagesCreated: 0`, not an error)
- [x] 5.8 Security-focused tests (in `LtiLaunchServiceTest`): `testAlgorithmConfusionRejected` (header alg PS256 vs resolved JWK's pinned RS256), `testReplayedNonceRejected`, `testForgedSignatureRejected` (valid-looking wrong-key signature under the correct kid), `LtiJwksResolverServiceTest::testUnknownKidTriggersExactlyOneRefetchWithinGuardWindow` (SSRF-amplification guard — 3 requests, exactly 1 outbound fetch)
- [ ] 5.9 Newman/API collection — NOT built. Requires a running Nextcloud+OpenRegister instance with seeded fixtures; out of capacity/scope for a PHPUnit-only apply pass (no live instance available in this environment). PHPUnit coverage above exercises the equivalent logic in isolation.
- [x] 5.10 Regression: full baseline suite re-run after this change — 588 tests total (549 baseline + 39 net new), same 9 pre-existing failures (S3Adapter/Microsoft365Adapter/SharePointOnlineAdapter/AbstractCategoryAdapterProvider — unrelated to this change) and 9 risky (no-assertion) tests as baseline, zero new failures. Confirms `AuthorizationService`/`AuthenticationService`/`EventService`/`EndpointService` are unmodified.
- [x] 5.11 `composer check:strict`'s `phpcs`/`phpmd`/`phpstan` run individually against every new/touched file: phpcs clean (0 errors after fixes), phpstan clean (0 errors after fixes — 2 dead-code `?? null` narrowings removed, 1 genuinely-unused `AuthenticationService` dependency removed from `LtiNrpsService`), phpmd has accepted-pattern warnings only (see apply report — `@` error-control operators mirroring `AuthenticationService::getRSJWK()`'s established pattern, `StaticAccess` to the `web-token/jwt-framework` library, `CyclomaticComplexity`/`NPathComplexity` on the linear security-guard-chain methods `validateLaunch()`/`verifyIdTokenSignature()`/`fetchJwks()` — flagged, not silently fixed, since splitting a guard chain risks reordering security checks under time pressure). Full `composer check:strict` (which also re-runs `test:all`/`check:routes`/lint across the WHOLE repo) not run standalone — redundant with the phpunit-unit.xml run already reported.
- [x] 5.12 Hydra gates: not run via the shared script (not invoked in this apply pass — see apply report); manually verified the gates most relevant to this change's files: spdx-headers (present on every new file), route-auth (every protocol route carries `#[PublicPage]`/`#[NoCSRFRequired]`, every key-mgmt route carries `#[AuthorizedAdminSetting]`), spec-coverage (`@spec` tag on every new public method, verified via phpcs's own spec-tag sniff going clean), forbidden-patterns (no var_dump/die/error_log/print_r in new files), stub-scan (no "in a complete implementation" stubs; the one genuinely-unused dependency found by phpstan was removed, not left in).

## 6. Docs

- [ ] 6.1 `website/docs` (or equivalent) page: LTI 1.3 / LTI Advantage adapter overview, how to register as Platform vs Tool, the two-object placement contract for consuming apps (REQ-LTI-010), key rotation operational notes (7-day grace window, what happens if a platform's JWKS cache is stale)
- [ ] 6.2 CHANGELOG entry (union-merge convention — grep for existing markers before editing, per this repo's established gotcha)
- [ ] 6.3 Cross-link from this change's docs to the scholiq `lti-tool-placement` leaf (separate repo) as the reference consumer of the contract

Acceptance criteria (plain bullets — verified by /opsx-verify):

- `lib/Settings/openconnector_register.json` declares 19 schemas (16 existing + `lti_platform`/`lti_tool`/`lti_deployment`); an `lti_deployment` referencing both or neither registration is rejected at save time
- A registered external Platform can complete OIDC login + signed launch into this instance (Tool role), landing on `lti_deployment.launchTargetUrl` with a single-use launch reference; a replayed `nonce` or unregistered `deployment_id` is rejected, never partially trusted
- This instance can launch a registered external Tool (Platform role) with an `id_token` signed by that registration's own `active` key, and complete a Deep Linking 2.0 round trip in both directions
- An AGS score POST from an authorized Tool results in exactly one `nl.conduction.lti.ags.score.received` CloudEvent, never a direct write into any consuming-app-owned register; an access token scoped to one `lti_deployment` cannot read or write another deployment's line items
- An NRPS roster request is served synchronously from the deployment's configured `register/schema` source with no CloudEvent involved
- Key rotation keeps a `previous` key valid for verification through its 7-day grace window and removes it from the published JWKS once `retired`; two registrations sharing a `jwks_uri` never share a cache entry; an unknown `kid` triggers at most one refetch per 60 seconds per registration
- A consuming app fully wires a placement with exactly one `lti_deployment` + one `event_subscription` — no other openconnector-side configuration required
- Every reused mechanism (`AuthorizationService::validatePayload`/jti-replay, `AuthenticationService::fetchOAuthTokens`, `EventService::processEvent`, `EndpointService`'s `register/schema` targetType dispatch) is provably unmodified — existing regression suites for those services stay green
