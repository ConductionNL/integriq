# openconnector-app-manifest

Chain D1 (config): Add src/manifest.json declaring openconnector's nav, pages, dependencies using the ConductionNL app-manifest schema (precedent: decidesk/src/manifest.json, ADR-024). Replaces imperative settings.js / navigation / widget JS files.
