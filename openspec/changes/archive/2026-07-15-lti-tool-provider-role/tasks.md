# Tasks — LTI Tool-role trust, identity-linking, and resource-mapping seam

> Builds strictly on top of the archived `lti-13-platform` change. No
> existing REQ-LTI-001..010 behaviour is modified. Implementation order:
> schema (nothing else compiles without the new fields/schema), then
> services, then the two new admin routes, then tests, then docs.

## 1. Register schema (lti-platform REQ-LTI-011/012/013)

- [x] 1.1 Add `status` (`pending | approved | suspended`, default `pending`) to `lti_platform` in `lib/Settings/openconnector_register.json`
- [x] 1.2 Add `status` (same enum/default) to `lti_tool`
- [x] 1.3 Add `identityPolicy` (`manualLinkOnly | autoProvisionAsRole`, default `manualLinkOnly`) and `defaultProvisionGroup` (nullable string, required when `identityPolicy: autoProvisionAsRole` via an `if/then` on `lti_platform`) — verified present and correct at HEAD
- [x] 1.4 Add `resourceLinkMappings` (array of `{resourceLinkId, targetType, targetId}`, mirroring `gradeSink`/`rosterSource`'s object shape) to `lti_deployment`
- [x] 1.5 Add new `lti_identity_link` schema: `uuid`, `ltiPlatformId` (FK, `x-openregister-onDelete: CASCADE`), `subject`, `userId`, `provisioningMethod` (`manual | auto`), `linkedByUserId` (nullable, set for `manual`), `linkedAt`, `created`, `updated`. Unique constraint via `configuration.unique: ["ltiPlatformId", "subject"]` — verified against `openregister/lib/Service/Object/ValidateObject.php:1918` (`checkUniqueness()`), which enforces the composite-field list at write time and raises a validation error naming both fields; this is the mechanism already used elsewhere in this repo (`grep -c 'x-openregister-onDelete'` = 9 including this one). `LtiIdentityLinkService::createLink()` also re-reads-on-conflict defensively for the racing-create case.
- [x] 1.6 Register `lti_identity_link` under `components.registers[openconnector].schemas`; count re-verified at HEAD = **21** (20 + `lti_identity_link`); `RegisterDescriptorTest::testRegisterDeclaresAllSchemaSlugs` updated to match (was asserting the pre-lti-identity-link 20-slug list and failed until fixed)
- [x] 1.7 `openspec sync` of the `openconnector-register-schema` delta into `openspec/specs/openconnector-register-schema/spec.md` — NOT done, left for the archive/sync step per this repo's OpenSpec workflow (same deferred-to-archive convention the archived lti-13-platform change used at its own §1.7); the synced spec must state 21, not 20

## 2. Backend services

- [x] 2.1 `LtiRegistrationResolverService::findPlatformByIssuer()`/`::findToolByClientId()`: filter to `status: approved` only via a shared `requireApproved()` gate; registrations found but not approved return `null` (same "not found" shape callers already handle); actual `status` logged at debug level. `findRegistrationByUuid()` (used by Platform-role launch initiation + Deep Linking signing, REQ-LTI-006) is gated identically — verified this extra coverage beyond the letter of this task, since an unapproved registration must not be usable to launch/sign through that path either
- [x] 2.2 `approve(registrationType, registrationUuid)` / `suspend(registrationType, registrationUuid)` landed on `LtiKeyService` (it already owns registration mutation via `saveObject()` for key lifecycle) via a shared private `transitionStatus()`
- [x] 2.3 New `LtiIdentityLinkService`: `resolveIdentity(ltiPlatformId, subject): array{status, userId}` implementing D2's manualLinkOnly/autoProvisionAsRole branches; auto-provisioning creates the Nextcloud user via `IUserManager::createUser()` (no existing provisioning helper found elsewhere in this codebase to reuse) with a deterministic uid derived from `(ltiPlatformId, subject)` (never from an unverified profile claim) and adds it to `defaultProvisionGroup` via `IGroupManager`; a misconfigured `autoProvisionAsRole` with no group fails closed to unlinked
- [x] 2.4 `LtiLaunchService::resolveResourceMapping(deploymentUuid, resourceLinkId): ?array` per REQ-LTI-013's exact-match → empty-default → null resolution order
- [x] 2.5 `CLAIM_RESOURCE_LINK` constant extracted alongside the existing `CLAIM_*` constants in `LtiLaunchService`

## 3. Endpoints

- [x] 3.1 `LtiController::approve()`/`suspend()`: `#[AuthorizedAdminSetting(OpenConnectorAdmin::class)]`, mirrors `generateKey()`/`rotateKey()`'s shape exactly
- [x] 3.2 `appinfo/routes.php`: two new routes, `POST /api/lti/{registrationType}/{registrationUuid}/approve` and `.../suspend`
- [x] 3.3 No new route for `LtiIdentityLinkService`/`resolveResourceMapping()` — both are consumed cross-app via PHP service injection. Precedent check: no OTHER app in this codebase currently consumes an OpenConnector Lti service this way yet (the scholiq `lti-tool-placement` leaf is a separate, not-yet-built repo/change) — this mirrors the *shape* `LtiLaunchService::consumeLaunchReference()`/`buildDeepLinkingResponse()` already establish for a same-pattern handoff, but is flagged here (per this task's own instruction) as not yet an exercised cross-app precedent, only an intra-service one

## 4. Tests

- [x] 4.1 PHPUnit: `LtiRegistrationResolverServiceTest` (new file, 12 tests) — `pending`/`suspended`/missing-`status` registrations resolve as not-found for `findPlatformByIssuer()`/`findToolByClientId()`/`findRegistrationByUuid()`; `approved` resolves normally
- [x] 4.2 PHPUnit: registration-trust-gate integration in `LtiLaunchServiceTest` — `testPendingPlatformLoginInitiationRejectedLikeUnregistered()`/`testPendingPlatformLaunchRejectedLikeUnregistered()` (resolver returns null, as the real gated resolver would for pending/suspended) reject with the exact same shape as `testUnregisteredIssuerRejectedBeforeRedirect()`
- [x] 4.3 PHPUnit: `LtiIdentityLinkServiceTest` (new file, 9 tests) — unlinked-under-manualLinkOnly (explicit and default-missing-policy) reports unlinked with no user created; manually-linked resolves without provisioning; first-seen subject under autoProvisionAsRole provisions into `defaultProvisionGroup`; repeat launches reuse the same provisioned user; misconfigured autoProvisionAsRole (no group) fails closed; two platforms sharing a `sub` resolve to independent links; unknown platform throws. Call-ordering scenario (REQ-LTI-012 scenario 4) covered as `LtiLaunchServiceTest::testForgedSignatureRejectedRegardlessOfIdentityPolicy()` — since `LtiIdentityLinkService` has no in-repo call site chained after `validateLaunch()` (consuming-app concern, cross-repo), this proves the achievable invariant: `validateLaunch()`'s own rejection is unaffected by `identityPolicy`/`defaultProvisionGroup` being present on the platform row
- [x] 4.4 PHPUnit: `resolveResourceMapping()` — exact match, empty-default fallback, `null` when unconfigured, `null` for an unknown deployment (4 tests added to `LtiLaunchServiceTest`)
- [x] 4.5 Regression: full baseline suite re-run, verbatim below — 0 new failures; all pre-existing REQ-LTI-001..010 tests pass unmodified. Two collateral fixes were needed to get there, both outside `lib/Service/Lti/`: (a) `RegisterDescriptorTest`'s hardcoded 20-slug expected list (1.6) — a straightforward fixture update; (b) the test-only stub `tests/stubs/.../ObjectService.php::saveObject()` had no `$_rbac`/`$_multitenancy` params at all (unlike its `find()`/`findAll()`, which already stub them) — `LtiIdentityLinkService::createLink()` is the first caller in this codebase to pass them to `saveObject()`, needed because this write (like this same service's own `findLink()`/`findPlatformData()` reads) happens with no interactive NC session, mirroring how the real OpenRegister `ObjectService::saveObject()` already supports the bypass. Widening the stub exposed a **pre-existing, latent bug** in `SynchronizationServiceTest::testSynchronizationLogWritesAreCreateOnly()`'s `saveObject` mock closure — it declared a phantom `$extend` 2nd parameter that has never existed on this stub, silently tolerated only because the untouched trailing position happened to fall back to the closure's own default; PHPUnit forwards a mock's FULL positionally-resolved parameter list (explicit + default-filled) to `willReturnCallback()`, not a named-argument-shaped one, so adding two trailing stub params shifted `true` (the new `$_rbac` default) into that closure's `$uuid` slot. Fixed by removing the phantom `$extend` parameter (now correctly 4 params, matching the stub). Audited every other `saveObject`-mocking test in the suite (`grep -rl "method('saveObject')" tests/`) for the same arity-mismatch risk — all others either use `willReturn`/`willReturnOnConsecutiveCalls` (arg-blind) or already declare the correct `($object, $register=null, $schema=null, $uuid=null)` shape, unaffected since PHP silently drops positional values beyond a closure's declared arity
- [x] 4.6 Scoped `phpcs`/`phpstan` (level 5) against every new/touched `lib/` file — clean (`lib/Service/Lti/LtiIdentityLinkService.php`, `LtiRegistrationResolverService.php`, `LtiLaunchService.php`, `LtiKeyService.php`, `lib/Controller/LtiController.php`); two pre-existing phpcs violations in touched files fixed in passing (a >150-char docblock line, an inline-comment capitalisation). `phpmd` on the same files surfaces several PRE-EXISTING, un-baselined findings (`validateLaunch()`/`verifyIdTokenSignature()` cyclomatic/NPath complexity, a couple of `ErrorControlOperator`/`StaticAccess`/`ElseExpression` items) all inside method bodies this change did not touch (`derivePublicJwk()`, `findDeployment()`, the crypto-verification chain itself) — left as-is rather than refactored, since restructuring the launch-validation control flow is explicitly out of scope ("do not weaken or reorder the crypto chain") and carries real regression risk; flagged for a separate follow-up. Full `composer check:strict` (which also needs a live NC/DB environment for parts of `test:all`) was not run — the scoped phpunit/phpcs/phpstan/phpmd above is what's achievable outside that environment, per this task's own instruction to run the strict PHPUnit command directly

## 5. Docs

- [x] 5.1 Extended `docs/features/lti-13-platform.md` with: the registration-trust-gate operational note, the identity-linking trust model in plain operator language, and the resource-link mapping seam for consuming-app authors — added as three new sections plus data-model/endpoint-table/"Not yet built" updates
- [x] 5.2 CHANGELOG entry added under `[Unreleased]` (checked for existing markers first — none for this change; the lti-13-platform entry was left untouched, new entry added above it)
- [x] 5.3 Cross-linked from the docs page ("Reference consumer" section) to the scholiq `lti-tool-placement` leaf as the intended consumer of `LtiIdentityLinkService`/`resolveResourceMapping()`

## Acceptance criteria (plain bullets — verified by /opsx-verify)

- An `lti_platform`/`lti_tool` registration defaults to `status: pending` and cannot complete a login/launch/token-issuance flow until an admin transitions it to `approved`; a `pending`/`suspended` registration is rejected with the exact same HTTP status/body as an unregistered issuer
- Under the default `manualLinkOnly` policy, a launch from a `sub` with no existing `lti_identity_link` row is reported unlinked and creates no Nextcloud user; under an explicit `autoProvisionAsRole` policy, a first-seen `sub` provisions a user into the configured `defaultProvisionGroup` and records `provisioningMethod: auto`
- Identity-linking policy has no effect on whether `validateLaunch()` accepts or rejects a launch — a forged/expired/replayed launch is rejected before any identity-linking code runs, regardless of policy
- `resolveResourceMapping()` returns the exact `resourceLinkId` match when configured, falls back to a deployment-default entry, and returns `null` when nothing is configured — it never performs the register/schema read itself
- Every REQ-LTI-001 through REQ-LTI-010 requirement and its existing test coverage remains unmodified and green
