# Tasks: nextcloud-forms-connector (superseded)

The original 10-task / 31-checkbox list was removed with the 2026-09-02
retirement (see proposal.md for the disposition; the list survives in
`archive/2026-07-15-nextcloud-forms-connector/tasks.md`, where 24/31 boxes
are checked with per-task evidence, and in git history). There is nothing
to implement from this change directly.

The two headings below are live `@spec` anchor targets (tags in `tests/`);
they keep their original text so the tags resolve.

### Task 9: Unit tests — Forms services

Shipped: `tests/Unit/Service/Forms/` at HEAD; evidence in the archived twin.

### Task 10: Integration tests — mocked `FormsClientInterface`, end-to-end dispatch

Shipped: `tests/Integration/Forms/` at HEAD; evidence in the archived twin.
